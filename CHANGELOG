CURRENT

API CHANGE
	converted ItemAttribute#DeletePolicy to an enum.
API CHANGE
	use generics for defining the type of ItemAttributes and
	EnumAttributes.
API CHANGE
	replaced JavaView#getValueGetterMethod by getValueGenericType
	this was a hack to make generics available in projects using JDK 1.5
	without making cope require JDK 1.5. This obsolete, since cope now
	requires JDK 1.5 anyway.

build 1777
