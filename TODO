* Instrumentor
	- first load all files, then write alles files
	- mehrsprachige generierte Comments
	- mehrsprachige Parametertypen
	- Instrumentierung nur für geänderte Dateien
	- disable/anderer access modifier für alle Methoden
* Lib
	- search API
		- oracle statistics
		- self joins
		- group by
	- Transactions: introduce timestamp and use it in
		* Item#touch
		* timestamp of data files
	- EnumAttribute: optionally put strings in DB
	- DataAttribute:
		0) test reverse proxy functionality of apache
		1) write tests for up/downloading data in copernica
		2) implement DataServlet
		4) remove mime type from data attribute, put this functionality into a pattern
		5) write tests for pure data attributes without mime type pattern
		6) replace Attribute#getColumns/#getMainColumn by #getColumn
		7) implement data attribute in database (BLOB)
	- HttpEntity:
		* globally use term Content-Type instead of Mime
		* allow Content-Type specified by EnumAttribute
		* maintain charset in Content-Type for texts
	- move Item#touch into separate pattern Timestamp
	- disable empty strings for oracle
		/ introduce database specific max/min length for strings
	- analog dsmf: Datenverzeichnisse prüfen
	- add/drop constraints:
		* alter table "SumItem" add constraint schnappel check ("num2" is not null)
		* alter table "ItemWithDoubleUnique" drop constraint "ItemWithDoublUniqu_str_Nn"
	- relation pattern
	- Vector: allow length terminated instead of null terminated as well
	- test model:
		* put classes for patterns into separate package
		* separate test model for lib tests and testwebapp (webtests)
		* make small test models for each lib test
	- move Cope#isNotNull(ObjectAttribute) to ObjectAttribute.isNotNull()

* Docs
	- aus AGENDA
* Copernica
	- query designer
	- schönere Darstellung der Qualifiers
	- Restriktionen
	- hochgeladene DataAttributes erst beim "Save" speichern
	- use <link rel="prev/next/copyright/home"
	- show warning on website, if account runs with empty/default/bad password
