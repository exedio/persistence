<ivysettings>
	<settings defaultResolver="default"/>
	<property
			name="shared.root"
			value="http://hudson.exedio.com/hudson/job/[module]/[revision]/artifact/main/build/success" />
	<resolvers>
		<ibiblio name="public" m2compatible="true"/>
		<url name="shared">
			<ivy      pattern="${shared.root}/ivy.xml" />
			<artifact pattern="${shared.root}/[artifact].[ext]"/>
			<artifact pattern="${shared.root}/[artifact]-[type].[ext]"/>
		</url>
		<filesystem name="sharedthirdparty">
			<ivy      pattern="/home/rw7/.sharedthirdparty/[organization]/[module]/[revision]/ivy.xml"/>
			<artifact pattern="/home/rw7/.sharedthirdparty/[organization]/[module]/[revision]/[artifact]-[type].[ext]"/>
		</filesystem>
		<chain name="main" dual="true">
			<resolver ref="sharedthirdparty"/>
			<resolver ref="shared"/>
			<resolver ref="public"/>
		</chain>
		<chain name="default" returnFirst="true" checkmodified="true">
			<resolver ref="main"/>
		</chain>
	</resolvers>
</ivysettings>
